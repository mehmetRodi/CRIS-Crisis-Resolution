# CRIS — Architecture Overview

A living, code-adjacent reference for the versioned repository. A local `crisismap.pdf`, when
supplied, provides product-design context but is not currently tracked. Accepted decisions are
recorded in [`adr/`](adr/). This document distinguishes the target design from what is wired
today.

## 1. Target system shape (design doc §3)

CRIS is designed as a **serverless AWS application**. Clients talk to a single AppSync
GraphQL API backed by DynamoDB. Expensive AI work is pushed onto an asynchronous queue so the
write path stays fast and resilient. The diagram is the end-state design; consult the current
implementation matrix below before assuming a component is operational.

```
        ┌──────────────────────────────────────────────┐
        │ React Native app (Citizen) · React SPA (Web) │  Volunteer · Responder · Coordinator
        └────────────────────┬─────────────────────────┘
                     │ HTTPS / GraphQL (+ subscriptions)
                     ▼
        ┌──────────────────────────┐        ┌───────────────┐
        │   AWS AppSync GraphQL     │◄───────│ Amazon Cognito│ (user pools + groups)
        └────────────┬─────────────┘        └───────────────┘
                     │ resolvers
                     ▼
        ┌──────────────────────────┐        ┌───────────────┐
        │        DynamoDB           │───────▶│  S3 (media)   │
        │   (system of record)      │ stream └───────────────┘
        └────────────┬─────────────┘
                     │ DynamoDB Streams → SQS (classification queue)
                     ▼
        ┌──────────────────────────┐
        │      Lambda workers       │ classify (Bedrock Triage Agent) · geocode
        │                           │ (Amazon Location) · score · dedupe
        └────────────┬─────────────┘
                     │ conditional write, then worker IAM call to publishReportUpdate
                     ▼
        ┌──────────────────────────┐        ┌───────────────┐
        │  AppSync subscriptions    │        │ SQS alert q → │ Alert Lambda → SNS
        │  (near real-time updates) │        │  SMS/email/push│
        └──────────────────────────┘        └───────────────┘
```

### Target flow (design doc §3, Fig 9)

1. Client submits a report through AppSync → written durably to DynamoDB → returned
   immediately as `NEW` (p95 < 800 ms).
2. A DynamoDB stream feeds an SQS queue; Lambda workers classify, geocode, score priority,
   and detect duplicates.
3. The worker writes results back conditionally, then calls `publishReportUpdate` over IAM so
   subscribed clients update in near real time (p95 < 2 s). The operation also retains the narrow
   `ADMIN` rule Amplify requires (ADR-0030).
4. Threshold-crossing incidents enqueue alerts delivered via SNS.

## 2. Report lifecycle (design doc §5.1)

State machine — every transition is role-checked, version-checked (optimistic lock), and
appends an immutable audit event. Encoded in code as `STATUS_TRANSITIONS` in
`packages/shared/src/domain.ts`.

```
NEW → PROCESSING → AI_CLASSIFIED → VERIFIED → IN_PROGRESS → RESOLVED
                        │              ▲                        │
                        │ low conf.    │ confirmed              │ reopen
                        ▼              │                        ▼
                 NEEDS_VERIFICATION ───┘                   IN_PROGRESS
                        │
                        ▼
                    REJECTED (terminal)
```

## 3. Design guarantees and service targets (design doc §1.1, §3.2)

These are requirements for the completed system, not claims that every path is implemented or
that the latency objectives have been measured in a deployed environment.

- A report is **never lost** once acknowledged, even if AI/geocoding/notifications are down
  (bounded retries, DLQs, defined terminal states; a Bedrock failure ⇒ `NEEDS_VERIFICATION`).
- Submission stays fast (p95 < 800 ms) — AI runs asynchronously.
- Coordinators get a single, de-duplicated, priority-ordered view (p95 < 2 s propagation).
- Public users see only **redacted** data; reporter identity/contact is protected.
- Priority is a **deterministic, explainable** score in [0, 10] mapped to bands P0–P3 — never
  raw model output.

## 4. Current implementation status

| Component                            | Current state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| React Native citizen app             | `apps/mobile` has optional sign-in, an Expo report form, image selection, and guest/authenticated `submitReport` wiring. Location capture is in place (CRIS-16, ADR-0034): GPS via `expo-location`, plus a MapLibre aim-and-confirm map pin and a free-text location hint. Because MapLibre needs native code, the app requires a custom Expo dev-client build and does not run in Expo Go. Picked photos upload to S3 via a presigned URL before submission (CRIS-17, ADR-0035). A failed or offline submission saves locally and retries once connectivity returns, surfaced via a persistent status banner (CRIS-26, ADR-0044). **Rebuilt in CRIS-57 (ADR-0057):** the hand-copied slate/blue `theme.ts` is replaced by a theme derived from `@crisismap/design`, the same package the web palette is checked against, and domain enums are rendered through shared labels instead of `replace(/_/g, ' ')`. The form is grouped required-first like web, and the submit control now follows ADR-0037 — it stays pressable, states what is outstanding, and moves focus to the field at fault. `react-native-svg` + `lucide-react-native` were added for icon parity, which makes a dev-client REBUILD mandatory. The app remains citizen-only: no operational workspace, and no mobile equivalent of the public incident map. It still has no automated test harness. |
| React SPA client                     | Rebuilt in CRIS-54 (ADR-0054/0055). Public routes: `/` (citizen landing), `/report`, and `/map` (public incident map). Operational routes live under `/workspace/*` in one role-adaptive shell — `/workspace` redirects each caller to the pane their role works from, `/workspace/incidents` is the map-first workspace (queue + live map + incident-detail rail) gated to `RESPONDER`/`COORDINATOR`/`ADMIN`, and `/workspace/tasks` is the redacted volunteer stage board gated to operational staff roles. `/coordinator` and `/volunteer` are kept as permanent redirects. The map now RENDERS the incident feed: severity-coloured clustered points, cluster colour taken from the most severe member, and selection shared with the queue and detail rail; the queue is its keyboard-operable twin. Role capability is derived in one place (`lib/capabilities.ts`) mirroring the server's own rules. Volunteers get a stage board rather than a map because the `VolunteerTask` projection carries no coordinates (ADR-0042). Styling is a semantic token layer with Tailwind's default palette replaced (ADR-0054). The incident-detail rail keeps CRIS-18 transitions, CRIS-32 assignment, the score breakdown, and the audit timeline; `/report` keeps the CRIS-26 offline save/retry behaviour.                                                               |
| Cognito auth                         | `apps/web/amplify/auth` defines email sign-in, five groups, guest Identity Pool access, and Cognito triggers that assign `CITIZEN` after self-sign-up and reconcile a transient assignment failure on the next authentication (CRIS-24, ADR-0041). Staff groups remain admin-assigned; no invite flow exists. `AuthContext` exposes the caller's ID-token groups as `roles`/`highestRole`, and `RequireRole` gates the coordinator and volunteer routes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| AppSync + DynamoDB                   | `apps/web/amplify/data` defines 11 persistent models, six `Report` GSIs, guarded custom mutations, `PublicReport`, the custom `VolunteerTask` query/type, and the guest-authorized `listPublicReports` query (ADR-0056) — the system's only unauthenticated data path. CRIS-24 removed blanket authenticated `Report` reads; ADR-0042 additionally removes generated model reads from volunteers and gives them the Lambda-backed redacted task projection instead. The projection's DynamoDB and GraphQL allow-lists exclude reporter data, raw text, media, notes, and precise coordinates. Other generated model operations remain coarse and coexist with guarded custom operations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| API contract + tests                 | `docs/api.md` documents custom operations, auth modes, stable errors, generated-model authorization, and known wire quirks. Handler-level adapter tests for all six custom operations run in normal CI; an explicitly acknowledged `npm run test:integration` suite provisions throwaway Cognito role users and exercises AppSync/Lambda/DynamoDB/S3 against a personal sandbox only (CRIS-29/32, ADR-0046/0049). `deploy.yml` now ends every deploy with a synthetic smoke transaction — one marked guest report must travel the full classification pipeline in the environment just deployed (CRIS-35, ADR-0051).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Write path (`submitReport`)          | `amplify/functions/submit-report` performs a durable `NEW` create, audit append, and client-request-id idempotency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| State transition mutation            | `amplify/functions/transition-report` implements role-checked, version-checked status transitions with audit events. The coordinator dashboard now invokes `updateReportStatus` from the incident-detail panel (client wrapper `lib/transition-report.ts` + `useReportTransition`), offering moves legal for the caller's real Cognito role (`callerRole`, CRIS-24), passing the read `version` as the optimistic lock, and surfacing `CONFLICT` (CRIS-18, ADR-0028, ADR-0041).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Team assignment mutation             | `amplify/functions/assign-team` implements the guarded `assignTeam` mutation (COORDINATOR/ADMIN-only): version-checked `Report.assignedTeamId` update, a new `Assignment` record, and an `ASSIGNED` audit event in one transaction. One active assignment is allowed per report; reassignment is deferred until the prior lifecycle can be retired atomically. Wired into the incident-detail panel (`lib/assign-team.ts` + `useAssignTeam`, team picker via `useTeams`), independent of the CRIS-18 status controls (CRIS-32, ADR-0049). Merging duplicate report groups remains deferred (no agreed contract, ADR-0038).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Public projection + real-time        | `PublicReport` is the redacted projection. All three direct report writers call `publishReportUpdate` after durable writes: `classify-report`, `transition-report`, and `assign-team` hold schema-level IAM `mutate` grants, and publish failures never roll back DynamoDB/audit commits. Amplify also requires a per-operation rule, so `ADMIN` remains the narrow client-facing mutation gate (CRIS-19, ADR-0009/0029/0030/0048/0049). Three authenticated custom subscriptions provide unfiltered, region-filtered, and status-filtered streams. The coordinator uses the unfiltered event as an invalidation signal and staff-reads only the affected report so `version` and internal detail stay authoritative; the volunteer board reconciles the redacted projection locally. CRIS-54 added the missing public baseline read: `listPublicReports` is a Lambda-backed, guest-authorized query that enforces BOTH the `PublicReport` field allow-list and a `PUBLICLY_VISIBLE_STATUSES` incident allow-list (`VERIFIED`/`IN_PROGRESS`/`RESOLVED` only) server-side, so unverified and rejected reports never reach the public map (ADR-0056). Anonymous map STREAMING is still deferred: Identity Pool guest auth remains unsupported on the required custom JS subscription handlers, so the public map is a polled snapshot with manual refresh.                 |
| AI classification contract + scoring | `packages/shared/src/classification.ts` contains the versioned v2 triage contract and deterministic score-v2 formula `clamp(U + C + A + V + R + D − uncertainty − staleness, 0, 10)` (CRIS-30, ADR-0047). The worker supplies event-time age, confidence, and extracted affected-population evidence; unknown evidence contributes zero. Fixed code weights keep a score version reproducible; unversioned `CategoryConfig` rows do not override them. The coordinator breakdown renders all positive factors and both penalties.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Async Bedrock pipeline               | DynamoDB Stream → EventBridge Pipe → SQS (+ DLQ) → Lambda → Bedrock Triage Agent is wired; both hops have separate dead-letter queues (ADR-0038). The worker uses Amazon Location Places geocoding by default with a null-geocoder fallback. Conservative duplicate detection scores nearby candidates and atomically links strong matches (≥ 0.80) into a shared `duplicateGroupId`. A successful link reads the complete chosen group through its GSI, then writes a second version-checked score snapshot plus `PRIORITY_SCORED` audit for the newly classified subject; known peers provide a conservative fallback while the eventually consistent GSI catches up. Existing-peer reconciliation, verification-triggered rescoring, scheduled staleness refresh, group merging, and surfacing the review band remain deferred.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Proximity-alert pipeline             | A second filtered Report-stream EventBridge Pipe durably enqueues newly threshold-crossing P0/P1 `AI_CLASSIFIED` reports (never `NEEDS_VERIFICATION`) onto an encrypted SQS queue using only `reportId` + `priorityBand`; `amplify/functions/alert-dispatch` re-reads the PII-free matching fields, discovers candidate `AlertSubscription`s via two independent, merged GSI lookups (`regionId` and a stored `centerGeohashPrefix`), narrows by category + minimum urgency + exact geofence radius, and delivers SMS (SNS) and email (SES). `AlertDelivery` uses a conditional attempt lease: `SENT` is terminal while `FAILED`/expired `PENDING` attempts can retry without resending successful channels. Amplify owner identities are normalized before Cognito `AdminGetUser`; malformed subscription filters conservatively do not match. Neither citizen client currently sets `Report.regionId`, so the geohash-prefix path is currently the practical discovery path. PUSH remains deferred (CRIS-34, ADR-0050/0053).                                                                                                                                                                                                                                                                                                                                           |
| S3 media                             | `apps/web/amplify/storage` defines a coarse report-media bucket access rule. Presigned upload is in place (CRIS-17, ADR-0035): the `createMediaUploadUrl` mutation mints a short-lived upload POST — with size and content-type enforced by the signed policy — keyed by `clientRequestId`, and both clients upload before `submitReport` and pass the resulting `mediaKeys`. No client holds direct bucket write. Read is group-scoped to `RESPONDER`/`COORDINATOR`/`ADMIN`; volunteers are excluded pending per-assignment delivery (ADR-0039). Quarantine/AV scanning, per-object ownership, signed delivery, and a lifecycle rule for unclaimed objects are still deferred.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Map                                  | `apps/web/src/surfaces/map` renders MapLibre with a live incident overlay (CRIS-54): a clustered GeoJSON source built from the redacted feed, severity-coloured points, a worst-member cluster colour, a selection halo drawn beneath the points, and a legend. Feature properties are an explicit PII-free allow-list (`incidentGeoJson.ts`). Reports without resolved coordinates are excluded and counted in the map's live region so their absence is stated rather than silent. Amazon Location map TILES are still not wired (`VITE_MAP_STYLE_URL` or public demo tiles); Places geocoding is tracked separately above. Both the incident map and the report form's location picker sit behind `React.lazy` boundaries, which keeps `maplibre-gl` (~285 kB gzipped) out of the entry chunk.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Shared domain vocabulary             | `packages/shared` provides statuses, roles, bands, categories, classification/scoring, authentication rules, report-form validation, and the `PUBLICLY_VISIBLE_STATUSES` public-map rule, used across clients and backend.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Shared visual vocabulary             | `packages/design` (CRIS-57, ADR-0057) provides the colour tokens, radius/spacing scales, and the human labels for every shared domain enum — consumed by both clients so they cannot drift. It is framework-free: icons and styling mechanisms stay platform-side. The web app's `styles/tokens.css` restates the palette because a browser needs literal CSS custom properties; `apps/web/src/styles/tokens.test.ts` parses that file and fails if it disagrees with the package in either direction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| CI/CD + web hosting                  | `ci.yml` runs formatting, lint, typecheck, build, and available workspace tests. After successful `main` CI, `deploy.yml` can run `ampx pipeline-deploy` through OIDC when `AWS_DEPLOY_ENABLED=true`, followed by the CRIS-35 backend smoke gate. That same exact checkout is then rebuilt with the generated production `amplify_outputs.json` and atomically uploaded to the existing app's Amplify Hosting `main` branch; `/` and the deep `/report` SPA route are probed before the run turns green (ADR-0059). The Amplify app deliberately has no source-repository connection, so Hosting cannot race the gated workflow. Source control does not reveal whether deployment is enabled. Rollback is a documented manual revert (`docs/runbooks/incident-response.md`, ADR-0051/0059).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Observability                        | AppSync and seven Lambdas (`submitReport`, `updateReportStatus`, `assignTeam`, `publishReportUpdate`, `classify-report`, `createMediaUploadUrl`, and `alert-dispatch`) have X-Ray tracing. CloudWatch alarms cover the resolver/support Lambdas, `alert-dispatch`, classification and alert DLQs, both stream-pipe DLQs, and AppSync 5XX errors. An SLA dashboard, structured logs, an ops SNS topic, synth assertions, and first-response guidance in `docs/runbooks/incident-response.md` are defined (ADR-0051/0052/0053). Public/guarded resolver alarms use PII-free `UnexpectedErrors` metrics so normal validation, authorization, legality, and optimistic-lock rejections do not page. The `listVolunteerTasks` and `listPublicReports` resolvers and the Cognito role-assignment trigger are alarmed but not yet traced; `listPublicReports` is the one function an unauthenticated caller can invoke, so its invocation and error curves are the first signal of abuse while WAF rate limiting (CRIS-25) is still open. The smoke gate samples submit→classified latency and verifies structured AI output per deploy; end-to-end alert delivery latency still lacks deployed client telemetry.                                                                                                                                                               |

CRIS-25's customer-managed key currently covers the triage queues and operations topic only.
DynamoDB reports and S3 media retain their existing service-managed encryption, and
application-layer reporter-contact encryption remains deferred. KMS data-key events are auditable,
but they are not a per-record access log because AWS services reuse data keys (ADR-0043).

## 5. Deferred to later phases (design doc §1.2, §4)

Phase 2: Verification Agent + Dispatch Agent, ChatOps (Slack/Teams), OpenSearch, SageMaker
Geospatial satellite verification. Future: CV damage assessment, evacuation routing,
cross-agency identity federation, active-active multi-region writes.
