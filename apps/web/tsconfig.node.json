{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "noEmit": true
  },
  // Build-time config only. `vitest.setup.ts` moved to `tsconfig.json` (CRIS-54)
  // because it polyfills DOM globals — `ResizeObserver`, `Element.prototype`,
  // `Storage` — for jsdom, and needs the DOM lib this Node project omits.
  "include": ["vite.config.ts"]
}
