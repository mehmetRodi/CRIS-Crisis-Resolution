{
  "name": "@crisismap/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types and constants for CRIS (report lifecycle, roles, priority bands).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
